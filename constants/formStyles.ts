import { StyleSheet } from 'react-native';

export const formStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  card: {
    marginBottom: 16,
  },
  formContainer: {
    gap: 8,
  },
  sectionTitle: {
    marginBottom: 12,
  },
  input: {
    marginBottom: 4,
  },
  submitButton: {
    marginHorizontal: 16,
    marginBottom: 18,
    marginTop: 3,
    height: 50,
    justifyContent: 'center',
  },
});
